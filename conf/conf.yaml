# {filepath} needs to be manually modified to the correct path before executing the program

# api server info
master: 127.0.0.1:8080
kubeconfig: /var/run/kubernetes/admin.kubeconfig

scheduler_policy: edgecloud
scheduler_policy_file: /var/run/kubernetes/edgecloud_policy.yaml